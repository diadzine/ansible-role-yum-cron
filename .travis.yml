---
sudo: required

services:
  - docker

env:
  - distribution: centos
    version: 7
    init: /usr/lib/systemd/systemd
    run_opts: "--privileged --volume=/sys/fs/cgroup:/sys/fs/cgroup:ro"

before_install:
  # Pull container.
  - 'docker pull ${distribution}:${version}'
  - 'sudo docker build --no-cache --rm --file=travis/Dockerfile.${distribution}-${version} --tag=${distribution}-${version}:ansible travis'

script:
  - container_id=$(date +%s)

  # Run container in detached state.
  - echo "Running container"
  - docker run --detach --volume="$PWD":/etc/ansible/roles/role_under_test:ro --name $container_id $run_opts ansible/${distro}-ansible:latest $init

  # Ansible syntax check.
  - echo "Checking role syntax"
  - docker exec --tty $container_id env ANSIBLE_FORCE_COLOR=1 ansible-playbook -i /etc/ansible/roles/role_under_test/tests/inventory /etc/ansible/roles/role_under_test/tests/travis.yml --syntax-check

  # Test role.
  - echo "Running the role"
  - docker exec --tty $container_id env ANSIBLE_FORCE_COLOR=1 ansible-playbook -i /etc/ansible/roles/role_under_test/tests/inventory /etc/ansible/roles/role_under_test/tests/travis.yml

  # Test role idempotence.
  - echo "Testing role idempotence"
  - idempotence_log=$(mktemp)
  - docker exec --tty $container_id env ANSIBLE_FORCE_COLOR=1 ansible-playbook -i /etc/ansible/roles/role_under_test/tests/inventory /etc/ansible/roles/role_under_test/tests/travis.yml | tee -a $idempotence_log
  - >
    tail $idempotence_log
    | grep -q 'changed=0.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
